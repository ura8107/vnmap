% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infrastructure.R
\name{infrastructure}
\alias{infrastructure}
\title{Obtain transport and logistics infrastructure}
\source{
OpenStreetMap contributors, Open Database License (ODbL) 1.0,
\url{https://www.openstreetmap.org/copyright}. The facility snapshot has OSM
base timestamp 9 July 2026 and retrieval date 11 August 2026. Network lines
use the checksum-pinned Geofabrik Viet Nam extract dated 10 August 2026.
}
\usage{
infrastructure(
  type = NULL,
  status = NULL,
  province = NULL,
  include = NULL,
  class = NULL,
  service = NULL,
  geometry = c("any", "point", "line"),
  crs = vnmap_crs()
)
}
\arguments{
\item{type}{Optional values among \code{"port"}, \code{"aerodrome"},
\code{"border_control"}, \code{"expressway"}, \code{"national_highway"}, and \code{"railway"}.}

\item{status}{Optional lifecycle status. Supported values are
\code{"operational"}, \code{"under_construction"}, \code{"planned"}, \code{"disused"},
\code{"abandoned"}, and \code{"unknown"}.}

\item{province}{Optional current province names or codes.}

\item{include}{Optional infrastructure IDs or names.}

\item{class}{Optional facility subclass, such as \code{"military_aerodrome"},
\code{"fishing_port"}, \code{"rail_yard"}, or \code{"rail_siding"}.}

\item{service}{Optional service classification, such as
\code{"commercial_service_candidate"}, \code{"service=yard"}, or \code{"usage=main"}.}

\item{geometry}{\code{"any"}, \code{"point"}, or \code{"line"}.}

\item{crs}{Output CRS.}
}
\value{
An \code{sf} object containing point and line geometry.
}
\description{
Obtain transport and logistics infrastructure
}
\details{
Coordinates are a pinned ODbL OpenStreetMap snapshot, not an
official register. Status is classified only when lifecycle tags state it;
otherwise it is \code{"unknown"}. No legal opening date is inferred.
\code{"aerodrome"} does not imply scheduled commercial service, and \code{"port"}
does not imply a seaport. Consult \code{infrastructure_class}, \code{status_source},
\code{location_accuracy}, and the retained raw OSM tag columns.

Trunk lines come from the checksum-pinned Geofabrik Vietnam OSM extract.
They are generalized to 100 metres for statistical graphics. Lifecycle
status is never inferred from road class or map presence.
Facility aliases and nearby border-control components are reconciled to
entity IDs. Points just outside generalized land geometry are retained only
within 15 km and identified in \code{country_relation}.
\code{province_code} is a scalar nearest-parent code for points only. It is \code{NA}
for lines, whose intersected parents are listed in pipe-delimited
\code{province_codes}. Province filtering always uses geometry intersection;
the point code is only a fallback for flagged near-boundary facilities.
}
\examples{
infrastructure(type = "aerodrome", province = "Ha Noi")
}
