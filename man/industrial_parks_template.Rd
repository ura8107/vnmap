% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/industrial-parks.R
\name{industrial_parks_template}
\alias{industrial_parks_template}
\title{Write a basic-information template for industrial parks}
\usage{
industrial_parks_template(path = NULL, blank = FALSE, ...)
}
\arguments{
\item{path}{Optional file to write. The extension selects the format:
\code{.csv} (default) or \code{.tsv}. When \code{NULL} the table is only returned.}

\item{blank}{When \code{TRUE}, the editable columns are written empty so that the
file records user-supplied values only. When \code{FALSE}, the bundled values
are written as a starting point.}

\item{...}{Filters passed to \code{\link[=industrial_parks]{industrial_parks()}}.}
}
\value{
A data frame, invisibly when \code{path} is supplied.
}
\description{
Produces the table that \code{\link[=industrial_parks]{industrial_parks()}} accepts through its
\code{attributes} argument: one row per park, an \code{id} column, and the columns a
user may fill in. Rows carrying an \code{id} that is already in the bundled layer
update that park; rows carrying a new \code{id} add a park, and must supply
\code{name_vi}, \code{province_code}, \code{longitude}, and \code{latitude}.
}
\details{
Only non-empty values are applied, so an untouched template changes nothing.
}
\examples{
template <- industrial_parks_template(province = "Dong Nai")
head(template[c("id", "name_vi", "status", "area_ha")])
}
