% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/administrative-crosswalk.R
\name{administrative_crosswalk}
\alias{administrative_crosswalk}
\title{Parse and query the 2025 administrative-unit crosswalk}
\usage{
administrative_crosswalk(path, current = NULL, former = NULL, province = NULL)
}
\arguments{
\item{path}{Path to the official NSO \code{.xlsx} workbook, normally obtained
with \code{\link[=download_administrative_crosswalk]{download_administrative_crosswalk()}}.}

\item{current}{Optional current commune names or five-digit codes.}

\item{former}{Optional former commune names or five-digit codes.}

\item{province}{Optional current or former provincial identifier accepted by
\code{\link[=province_code]{province_code()}}. Rows matching either geography are retained.}
}
\value{
A data frame. The \code{relation} column is \code{"whole"} or \code{"part"}.
Reconciliation with the bundled 2026 snapshot is reported in
\code{current_code_status} and in the \code{crosswalk_only_codes} and
\code{current_snapshot_only_codes} attributes.
}
\description{
Parse and query the 2025 administrative-unit crosswalk
}
\details{
This table is inherently many-to-many: a former unit may be split
among current units, and a current unit may combine several former units.
It is a membership crosswalk, not an allocation-weight table. The source
publishes no population or area shares, so none are inferred.

The workbook describes the July 2025 transition while bundled current
boundaries are a 25 July 2026 observed snapshot. Status fields expose
differences rather than silently coercing codes between vintages. No
per-code legal effective date is assigned.
}
\examples{
\dontrun{
path <- download_administrative_crosswalk(tempfile(fileext = ".xlsx"))
administrative_crosswalk(path, current = "00004")
}
}
