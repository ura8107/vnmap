% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/industrial-park-registry.R
\name{industrial_park_registry}
\alias{industrial_park_registry}
\title{Obtain the national industrial-park registry}
\usage{
industrial_park_registry(
  province = NULL,
  status = NULL,
  category = NULL,
  accuracy = NULL,
  include = NULL
)
}
\arguments{
\item{province}{Optional current or pre-2025 provincial identifiers.}

\item{status}{Optional status values: \code{"operational"},
\code{"under_construction"}, \code{"established_not_started"}, or \code{"unknown"}.}

\item{category}{Optional category values: \code{"industrial_park"},
\code{"export_processing_zone"}, \code{"high_tech_park"}, or \code{"industrial_cluster"}.}

\item{accuracy}{Optional location accuracy: \code{"site"} for records with a
mapped boundary, \code{"locality"} for records placed by a nearby reference.}

\item{include}{Optional park identifiers or Vietnamese/English names.}
}
\value{
A data frame. See \link{industrial_parks_data} for the column contract
and data provenance.
}
\description{
Returns the bundled registry of Vietnamese industrial parks as a plain data
frame, one row per park, without requiring \code{sf}. Every row carries a
representative \code{lon}/\code{lat}, the administrative units it falls in, and the
OpenStreetMap elements it was built from. Use \code{\link[=industrial_parks]{industrial_parks()}} for the
same records with their boundaries attached.
}
\examples{
registry <- industrial_park_registry(province = "Dong Nai")
head(registry[c("id", "name_vi", "commune_name", "area_ha")])

# Parks placed only by a nearby gate, bus stop or internal road.
nrow(industrial_park_registry(accuracy = "locality"))
}
\seealso{
\code{\link[=industrial_parks]{industrial_parks()}}, \code{\link[=industrial_park_coverage]{industrial_park_coverage()}}
}
