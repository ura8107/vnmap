% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/economic-zones.R
\name{geom_economic_zones}
\alias{geom_economic_zones}
\title{Add economic and policy zones to a ggplot map}
\usage{
geom_economic_zones(
  data = NULL,
  province = NULL,
  type = NULL,
  status = NULL,
  include = NULL,
  as_of = NULL,
  include_unknown = TRUE,
  geometry = c("best", "polygon", "point"),
  crs = vnmap_crs(),
  polygon_fill = "#6a51a3",
  polygon_colour = "#3f007d",
  point_colour = "#54278f",
  alpha = 0.3,
  linewidth = 0.3,
  point_size = 2
)
}
\arguments{
\item{data}{Optional economic-zone \code{sf} object.}

\item{province}{Optional current or pre-2025 province identifiers.}

\item{type}{Optional zone types: \code{"coastal_economic_zone"},
\code{"border_gate_economic_zone"}, \code{"export_processing_zone"}, or
\code{"national_high_tech_park"}.}

\item{status}{Optional legal-status values: \code{"established"} or
\code{"candidate_or_count_reconciled"}. The latter means that an official
aggregate count or non-establishment reference supports inclusion, but a
row-level establishment instrument has not been verified.}

\item{include}{Optional IDs, Vietnamese/English names, or aliases.}

\item{as_of}{Optional date. Zones whose verified establishment effective
date is later than this date are excluded.}

\item{include_unknown}{With \code{as_of}, retain records whose establishment
effective date is unknown. Defaults to \code{TRUE} so catalogue records are not
silently lost; set to \code{FALSE} for strict dated panels.}

\item{geometry}{One of \code{"best"}, \code{"polygon"}, or \code{"point"}. \code{"point"}
converts polygon zones to an interior point. The initial conservative
snapshot contains deterministic province-only representative points.}

\item{crs}{Coordinate reference system understood by \code{\link[sf:st_transform]{sf::st_transform()}}.}

\item{polygon_fill, polygon_colour}{Polygon colours.}

\item{point_colour}{Representative-point colour. Province-only fallback
points use a cross (\code{shape = 4}) to distinguish them from site locations.}

\item{alpha}{Polygon opacity.}

\item{linewidth}{Polygon outline width.}

\item{point_size}{Representative-point size.}
}
\value{
A list of \code{ggplot2} layers.
}
\description{
Add economic and policy zones to a ggplot map
}
\examples{
plot_vnmap() + geom_economic_zones(type = "national_high_tech_park")
}
