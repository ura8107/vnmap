% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/industrial-parks.R
\name{write_industrial_parks}
\alias{write_industrial_parks}
\title{Write industrial park data to a file}
\usage{
write_industrial_parks(x, path, format = NULL, ...)
}
\arguments{
\item{x}{An \code{sf} object from \code{\link[=industrial_parks]{industrial_parks()}}.}

\item{path}{Output file. The extension selects the format unless \code{format}
is given.}

\item{format}{One of \code{"csv"}, \code{"geojson"}, or \code{"gpkg"}.}

\item{...}{Passed to \code{\link[sf:st_write]{sf::st_write()}} for the spatial formats.}
}
\value{
\code{path}, invisibly.
}
\description{
Exports an industrial park layer for use outside R. CSV output drops the
geometry column and adds representative \code{longitude} and \code{latitude} columns,
so it round-trips through \code{\link[=industrial_parks]{industrial_parks()}}'s \code{attributes} argument;
GeoJSON and GeoPackage output keep the mapped boundaries.
}
\examples{
parks <- industrial_parks(province = "Dong Nai")
file <- tempfile(fileext = ".csv")
write_industrial_parks(parks, file)
}
