% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/industrial-park-registry.R
\name{industrial_park_coverage}
\alias{industrial_park_coverage}
\title{Report industrial-park mapping coverage}
\usage{
industrial_park_coverage(scope = c("province", "national"))
}
\arguments{
\item{scope}{\code{"province"} for mapped records per current province, or
\code{"national"} to compare the registry against the official national totals.}
}
\value{
A data frame. For \code{scope = "province"}: \code{province_code},
\code{province_en}, \code{parks}, \code{with_boundary}, \code{locality_only}, and
\code{mapped_area_ha}, with one row per current provincial unit including those
holding no mapped park. For \code{scope = "national"}: one row per official
metric with the official \code{value}, the comparable \code{mapped} figure where one
exists, and the cited source.
}
\description{
Compares the bundled registry with the official count of established
industrial parks. The registry is a conservative snapshot: a park is recorded
only where a redistributable mapped location exists, so it is smaller than
the official register and unevenly complete across provinces. This function
makes that gap explicit rather than leaving it implied.
}
\examples{
head(industrial_park_coverage())
industrial_park_coverage("national")[1:3, c("metric", "value", "mapped")]
}
\seealso{
\code{\link[=industrial_park_registry]{industrial_park_registry()}}
}
