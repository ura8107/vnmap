% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vn-map.R
\name{vn_map}
\alias{vn_map}
\title{Obtain Viet Nam map data}
\usage{
vn_map(
  geography = c("provinces", "communes", "provinces_63", "districts_63", "communes_63"),
  include = NULL,
  region = NULL,
  province = NULL,
  crs = vnmap_crs()
)
}
\arguments{
\item{geography}{One of \code{"provinces"} (the 34 units effective from July
2025), \code{"communes"} (current commune-level units),
\code{"provinces_63"} (the pre-July 2025 units), \code{"districts_63"} (the
pre-July 2025 district-level units), or \code{"communes_63"} (the pre-July 2025
commune-level units). See Details for the optional lower-level layers.}

\item{include}{Optional character vector of names, aliases, official
two-digit codes, or ISO 3166-2 codes to retain. Vietnamese names may be
supplied with or without diacritics.}

\item{region}{Optional character vector of socio-economic region codes
(\code{"RRD"}, \code{"NMM"}, \code{"NCC"}, \code{"CH"}, \code{"SE"}, \code{"MRD"}) or English region
names used to retain whole regions. Units matching either \code{include} or
\code{region} are kept. Available only for the provincial geographies.}

\item{province}{Optional character vector of provincial-level identifiers
(names, aliases, or codes) used to keep only the lower-level units that
fall inside those provinces. Available for \code{"communes"},
\code{"districts_63"}, and \code{"communes_63"}; historical layers resolve against
the 63-unit geography.}

\item{crs}{Coordinate reference system understood by \code{\link[sf:st_transform]{sf::st_transform()}}.}
}
\value{
An \code{sf} object with one row per unit. Provincial layers carry
\code{code}, \code{iso}, \code{name_vi}, \code{name_en}, \code{type}, and \code{geometry}; current units
created by the 2025 reform have \code{NA} in \code{iso} where no code is bundled.
The lower-level layers carry \code{code}, \code{name_vi}, \code{name_en}, \code{province_code},
\code{province_en}, and \code{geometry}.
}
\description{
Returns a simple-features boundary layer for the provincial-level
administrative units of Viet Nam. The current layer contains the 34 units
effective from 1 July 2025. A 63-unit layer is retained for data collected
under the immediately preceding geography.
}
\details{
The bundled geometry is projected to EPSG:3405 by default. Pass a
different EPSG code or an \code{sf} CRS object to \code{crs} when another projection
is required. Filtering happens before transformation.

The current 34-unit layer is dissolved from the exact same current
commune source described below, so parent and child boundaries share the
same 25 July 2026 observed snapshot. Historical \code{"provinces_63"} geometry
remains derived from geoBoundaries. All geometry is intended for
statistical graphics, not surveying, navigation, or legal determinations.

The bundled \code{"communes"} layer contains the 3,321 current commune-level
units in the 25 July 2026 catalogue snapshot. It carries validity,
geography-vintage, source, and geometry-accuracy columns. Its boundaries
are generalized to 100 metres for statistical graphics.

The optional \code{"districts_63"} (second-level) and \code{"communes_63"}
(third-level) layers hold the district and commune units as they existed
before the July 2025 reform, which abolished the district tier and merged
communes. They are historical only, are not bundled by default because of
their size, and are generated by \code{data-raw/build_adm2.R} and
\code{data-raw/build_adm3.R}; \code{\link[=vn_map]{vn_map()}} raises an informative error when the
requested layer has not been built. The commune layer contains many
thousands of units, so filtering with \code{province} (or \code{include}) is
strongly recommended rather than drawing it nationwide.

This function requires the \pkg{sf} package. Name and code lookups
(\code{\link[=province_code]{province_code()}}, \code{\link[=province_info]{province_info()}}, \code{\link[=province_region]{province_region()}}) work without
it.
}
\examples{
map <- vn_map()
nrow(map)
names(map)

north <- vn_map(include = c("Ha Noi", "Bac Ninh"))
nrow(north)

delta <- vn_map(region = "MRD")
nrow(delta)

historical <- vn_map("provinces_63", crs = 4326)
sf::st_crs(historical)$input

\dontrun{
# Lower-level layers must be built first (see data-raw/build_adm3.R).
hanoi_communes <- vn_map("communes_63", province = "Ha Noi")
}
}
\seealso{
\code{\link[=plot_vnmap]{plot_vnmap()}}, \code{\link[=province_code]{province_code()}}, \code{\link[=province_info]{province_info()}}
}
